import {Request, Response} from 'express';
import { UserRepository } from '../../entities/user.repository';

const UserControllers: UserRepository = {
    getAllUsers() {
        
    },

    getUserById(id) {
        
    },

    createNewUser(User) {
        
    },

    modifyUser(user, id) {
        
    },

    deleteUSer(id) {
        
    },
}

export default UserControllers